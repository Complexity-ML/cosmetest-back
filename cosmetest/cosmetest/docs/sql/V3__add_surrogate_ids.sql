-- Expansion locale : ajout d'identifiants techniques sans supprimer les données métier.
-- Prérequis : dump restaurable vérifié et préflight exécuté.

ALTER TABLE etude_volontaire
    ADD COLUMN ID_ETUDE_VOLONTAIRE BIGINT UNSIGNED NOT NULL AUTO_INCREMENT FIRST,
    ADD PRIMARY KEY (ID_ETUDE_VOLONTAIRE);

ALTER TABLE rdv
    ADD COLUMN RDV_PK BIGINT UNSIGNED NOT NULL AUTO_INCREMENT FIRST,
    DROP PRIMARY KEY,
    ADD PRIMARY KEY (RDV_PK),
    ADD UNIQUE KEY uk_rdv_etude_numero (ID_ETUDE, ID_RDV);

ALTER TABLE infobancaire
    ADD COLUMN ID_INFOBANCAIRE BIGINT UNSIGNED NOT NULL AUTO_INCREMENT FIRST,
    ADD PRIMARY KEY (ID_INFOBANCAIRE);

ALTER TABLE preetudevol
    ADD COLUMN ID_PREETUDEVOL BIGINT UNSIGNED NOT NULL AUTO_INCREMENT FIRST,
    ADD PRIMARY KEY (ID_PREETUDEVOL);
